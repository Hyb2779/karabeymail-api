{
  "name": "public-email-api",
  "version": "1.0.0",
  "description": "Public transactional email API — Maileroo wrapper",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "gen-token": "node scripts/generate-token.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
